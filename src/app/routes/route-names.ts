export enum FlowName {
  noflow = "noflow",
  checkin = "checkin",
  booking = "booking"
}

export enum PageName {
  nopage = "noflow",
  select = "select",
  passengers = "passengers",
  extras = "extras",
  seatmap = "seatmap",
  payment = "payment",
  finalize = "finalize",
  confirmation = "confirmation",
  review = "review"
}
